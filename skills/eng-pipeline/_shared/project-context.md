# Project context (fill once per repo)

- **Stack:** <!-- e.g. Laravel 11 + Inertia/React, PostgreSQL, Redis -->
- **Run tests:** `<!-- command -->`
- **Lint / format:** `<!-- command -->`
- **Type check:** `<!-- command -->`
- **Local dev:** `<!-- command -->`
- **Branch naming:** `<type>/<ticket>-<slug>` (feat, fix, chore, refactor)
- **Commit style:** Conventional Commits
- **Artifacts dir:** `docs/work/<ticket>/`
- **Human approvers:** spec `approved` → <name>; QA `SHIP` → <name>
- **Non-negotiables:** <!-- e.g. no raw SQL in controllers, money in integer cents, flags via X -->

If any field is empty, ask before Stage 5 (Develop).
